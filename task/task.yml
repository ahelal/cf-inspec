---
platform: linux

image_resource:
  type: docker-image
  source:
    repository: chef/inspec
    tag: stable

inputs:
- name: tests

params:
  OM_TARGET:
  OM_USERNAME:
  OM_PASSWORD:
  OM_SKIP_SSL_VALIDATION:

run:
  path: inspec tests/$TESTS_PATH

